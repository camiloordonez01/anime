type Query {
    anime(id: Int!): anime
    animes(type: String): [anime]!
    import(id: Int): String
}

type anime {
    id: Int!
    name: String!
    genre: [String]
    type: String
    episodes: Int!
    ratiung: String
    members: Int
}